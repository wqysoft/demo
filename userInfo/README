bootstrap样式
HTML5
v-on指令
 add:function(){
          this.myData.push({
           name:this.username,
           age:this.age
          });
         this.username="";
         this.age=""
         nowIndex:0
        },
        deleteMsg:function(n){
            if(n==-2){
                this.myData=[];
            }else
            {this.myData.splice(n,1);}
            }     
